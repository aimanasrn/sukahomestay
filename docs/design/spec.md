# SUKA HOMESTAY design system

Generated concepts: home-concept.png, property-concept.png, booking-admin-concept.png.
The first concept governs the homepage: 88px ivory header, full-width 515px photographic hero, left white serif headline, subtle left forest overlay, floating ivory availability bar, then an editorial four-column accommodation introduction. Lower sections use the property concept's sage band, selectable main/studio diagram and comparison table. Booking/admin use the third concept's numbered stepper, outlined selection rows, ivory summary and restrained sidebar/table.

Tokens: ivory #FAF7F2; forest #254B40; sage #A8B8A0; sand #E9DDCB; terracotta #C97956; charcoal #252A27. Playfair Display 400/500 for headings, DM Sans 400–700 for UI. Desktop gutters 5.2vw/max 1360px. Radius 8–12px. Lucide outline icons 18–22px/1.5 stroke. Solid forest CTA, underlined editorial links, subtle borders; no invented hero eyebrow. Mobile: collapsed nav, stacked search, single-column cards and booking fields, compact sticky booking total. Motion 180–350ms with reduced-motion override.

Allowed first-viewport copy comes from the user's hero/nav/search labels and the home concept. Intentional corrections: concept mixed English card names into BM and invented example dates; implementation uses fully translated names and user-selectable current dates. Rates clearly marked sample. No generated concept claims about private entrances, guest capacity, tax, privacy, or facilities will be copied unless present in the brief. Browser demo notice is a functional disclosure required to prevent simulated bookings being mistaken for real requests. Diagram uses semantic selectable buttons and simple house icons rather than the concept's decorative architectural illustration.

Standalone generated courtyard, bedroom and living-room assets are illustrative; all are explicitly labeled. Asset prompts: a warm tropical Malaysian ivory bungalow courtyard with sage shutters, rattan veranda and leafy frangipani; a matching cream/sage bedroom with white linen; and a matching airy living room with rattan furniture and garden-facing windows. Built-in image generation used. No property photographs or facts claimed.

## Final visual verification — 7 September 2026

Reference concepts are `docs/design/home-concept.png`, `docs/design/property-concept.png` and `docs/design/booking-admin-concept.png`. These establish the implementation direction; no separate user design approval is implied.

The in-app browser rendered the application at the home concept's native **1536 × 1024** viewport and at **390 × 844** for mobile. Current-browser rendering was also checked. The desktop document occupies 1521 pixels after its native scrollbar. The browser's viewport-only capture clipped the right edge, so final evidence uses a full-page browser screenshot cropped to the first 1521 × 1024 pixels, with no visual retouching. `view_image` inspected the concepts and corresponding implementation captures, including a final paired inspection of the home reference and latest render.

Evidence directory: `C:/Users/ASUS/.codex/visualizations/2026/09/07/01a079ce-d98f-76b0-9173-e9df35248aa7/`. Files: `home-desktop-final.png`, `home-mobile.png`, `property-final.png`, `booking-desktop.png`, `booking-mobile.png`, and `admin-final.png`.

| Comparison | Concept and render evidence | Resolution |
| --- | --- | --- |
| First-viewport copy | Home reference and final home render retain the supplied BM hero, description, navigation, search labels and availability CTA. | Copy diff checked. Localized accommodation names replace mixed English labels; real date inputs replace invented concept dates. Image/sample-rate disclosures remain intentional. No new hero eyebrow was added. |
| Composition and spacing | Both home images use an 88px ivory header, 515px full-width hero, left headline, overlapping search and an introduction beside three accommodation cards. | Preserved the structure and alignment. Cards include the brief's sample prices, extending below the initial viewport. |
| Typography | Home render uses Playfair Display headings and DM Sans controls/body. | Increased desktop navigation, body, search controls and card typography after comparison; mobile retains deliberate wrapping and compact controls. |
| Palette and imagery | Home concept and render use ivory, forest, sage and restrained terracotta, with a left forest hero overlay. | Preserved palette/overlay direction. Independently generated property illustrations intentionally differ from concept photography and are labelled. Main card now uses a distinct living-room image. |
| Diagram and table | Property concept and `property-final.png` show the main home and three roomstays, selected states and bedroom/bathroom totals. | Accessible buttons replace decorative architectural art. Each roomstay has its own comparison row so configurable amenities remain accurate. All selected yields Whole House, 7 bedrooms and 6 bathrooms. |
| Booking and admin | Booking/admin concept and respective screenshots show a stepper, selection rows, price summary, sidebar, statistics and booking table. | Full usable pages replace the concept's two-screen presentation. Explicit demo disclosures and honest empty states are intentional functional additions. |
| Mobile and accessibility | Mobile screenshots show stacked fields/cards, menu and compact booking total. | Restored the guest search input on mobile; all four search fields remain available. No horizontal page overflow at 390px. Route/step focus preserves scroll position; reduced-motion styles are supplied. |
| Icons and controls | Outline icons throughout navigation, search, card counts, calendar, status and admin actions follow the concept's restrained visual system. | Consistent Lucide components, optical sizing and clear selected/disabled states; code-native labels and controls remain interactive. |

The chosen visual system was faithfully verified against the concept images with these documented intentional differences. No material unresolved clipping, overflow, missing imagery or broken primary controls remained in the checked views. This is a fidelity assessment, not a claim of pixel-identical imagery.

Functional verification: production build, six Vitest tests, 23 isolated PostgreSQL scenarios and Edge Function Deno type-check passed. In-app browser checks covered booking → language switch with fields preserved → add-on selection → submission → saved WhatsApp copy → admin payment → confirmation, filters, resource calendar, detail pages and mobile navigation. Fictional demo records were cleared afterwards. The supplied Playwright suite was listed successfully but its automated runner was not executed; live Supabase Auth/Edge/Turnstile/cron verification remains dependent on configuration.
