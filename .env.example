# Copy server/.env.example to server/.env and client/.env.example to client/.env
# Do not store real secrets in this file.
# Manual reservation settings are documented in server/.env.example, including
# ADMIN_WHATSAPP_NUMBER, PENDING_APPROVAL_EXPIRY_HOURS,
# AWAITING_PAYMENT_EXPIRY_HOURS, and DATA_ENCRYPTION_KEY.
