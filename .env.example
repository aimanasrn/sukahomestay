# Copy server/.env.example to server/.env and client/.env.example to client/.env
# Do not store real secrets in this file.
# Supabase credentials and manual reservation settings are documented in the
# two scoped examples. Only the publishable key belongs in the client file.
