import { notFound } from "next/navigation"; const pages:Record<string,{title:string;copy:string}>={gallery:{title:"A closer look at SukaHomestay",copy:"Browse the exterior, shared spaces and details that make your stay comfortable."},about:{title:"A welcoming place to stay",copy:"SukaHomestay is a locally hosted accommodation designed for comfortable family and group stays."},"how-it-works":{title:"How booking works",copy:"Check availability, request your stay, then upload your payment proof for confirmation."},reviews:{title:"Guest reviews",copy:"Approved guest reviews will appear here once they are available."}};
export default async function Page({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const page=pages[slug];if(!page)return notFound();return <section className="section"><div className="shell"><span className="eyebrow">SukaHomestay</span><h1 className="title">{page.title}</h1><p className="lead">{page.copy}</p><div className="hero-image" style={{marginTop:30}}><img src="/images/sukahomestay-hero.png" alt="SukaHomestay"/></div></div></section>}
