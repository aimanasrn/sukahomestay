"use client"; import { FormEvent, useState } from "react";
export default function Contact(){const [sent,setSent]=useState(false);function submit(e:FormEvent){e.preventDefault();setSent(true)}return <section className="section"><div className="shell"><span className="eyebrow">Get in touch</span><h1 className="title">We are here to help plan your stay.</h1><p className="lead">For booking questions and special requests, send a message and our host will respond.</p>{sent?<div className="amenity" role="status">Thank you — your enquiry has been received.</div>:<form className="form" onSubmit={submit}><input required aria-label="Full name" placeholder="Full name"/><input type="email" required aria-label="Email" placeholder="Email address"/><input required aria-label="Phone" placeholder="Phone number"/><input required aria-label="Subject" placeholder="Subject"/><textarea required aria-label="Message" placeholder="How can we help?"/><button className="button">Send enquiry</button></form>}</div></section>}
