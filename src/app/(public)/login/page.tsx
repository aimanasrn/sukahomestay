"use client";
import Link from "next/link"; import { FormEvent, useState } from "react"; import { usePathname, useRouter, useSearchParams } from "next/navigation"; import { createClient } from "@/lib/supabase/browser";
export default function AuthPage(){const signup=usePathname()==="/signup",router=useRouter(),search=useSearchParams(),[message,setMessage]=useState(""),[loading,setLoading]=useState(false);async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();setLoading(true);setMessage("");const form=new FormData(e.currentTarget),email=String(form.get("email")),password=String(form.get("password")),supabase=createClient();const result=signup?await supabase.auth.signUp({email,password,options:{data:{full_name:String(form.get("name"))}}}):await supabase.auth.signInWithPassword({email,password});setLoading(false);if(result.error){setMessage(result.error.message);return}if(signup){setMessage("Account created. You can now log in.");return}const {data:{user}}=await supabase.auth.getUser();if(!user){setMessage("Unable to verify your session.");return}const {data:profile}=await supabase.from("profiles").select("role").eq("id",user.id).single(),next=search.get("next");router.push(next?.startsWith("/")?next:profile?.role==="admin"?"/admin":"/dashboard")}return <section className="section"><div className="shell"><span className="eyebrow">SukaHomestay account</span><h1 className="title">{signup?"Create your account":"Welcome back"}</h1><form className="form" onSubmit={submit}>{signup&&<input name="name" required placeholder="Full name"/>}<input name="email" type="email" required placeholder="Email address"/><input name="password" type="password" minLength={8} required placeholder="Password (minimum 8 characters)"/><button className="button" disabled={loading}>{loading?"Please wait…":signup?"Create account":"Log in"}</button></form>{message&&<p role="status">{message}</p>}<p>{signup?"Already have an account?":"New to SukaHomestay?"} <Link href={signup?"/login":"/signup"}>{signup?"Log in":"Create an account"}</Link></p></div></section>}
