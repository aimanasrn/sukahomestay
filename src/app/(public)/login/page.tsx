"use client"; import Link from "next/link"; import { FormEvent, useState } from "react";
export default function Login(){const [message,setMessage]=useState("");function submit(e:FormEvent){e.preventDefault();setMessage("Connect Supabase environment variables to enable secure login.")}return <section className="section"><div className="shell"><h1 className="title">Welcome back</h1><form className="form" onSubmit={submit}><input type="email" required placeholder="Email address"/><input type="password" required placeholder="Password"/><button className="button">Log in</button></form>{message&&<p role="status">{message}</p>}<p>New to SukaHomestay? <Link href="/signup">Create an account</Link></p></div></section>}
