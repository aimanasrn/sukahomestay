import { notFound } from "next/navigation"; import Image from "next/image"; import Link from "next/link"; import { units } from "@/data/units";
export default async function UnitPage({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const unit=units.find(x=>x.slug===slug);if(!unit)return notFound();return <section className="section"><div className="shell split"><div><span className="eyebrow">{unit.type.replace("_"," ")}</span><h1 className="title">{unit.name}</h1><p className="lead">{unit.shortDescription}</p><h3>What is included</h3><div className="amenities">{unit.amenities.map(item=><div className="amenity" key={item}>✓ {item}</div>)}</div><p className="price">From RM {unit.basePrice} / night</p><Link className="button" href={`/booking?unit=${unit.slug}`}>Reserve this stay</Link></div><div className="hero-image"><Image src={unit.image} width={1600} height={1000} alt={unit.name}/></div></div></section>}
