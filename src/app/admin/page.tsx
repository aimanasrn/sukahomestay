"use client";
import { useEffect, useState } from "react"; import { readBookings, updateBooking, type LocalBooking } from "@/lib/demo-bookings";
export default function Admin(){const [bookings,setBookings]=useState<LocalBooking[]>([]);const refresh=()=>setBookings(readBookings());useEffect(()=>{refresh();window.addEventListener("sukahomestay-bookings",refresh);return()=>window.removeEventListener("sukahomestay-bookings",refresh)},[]);return <><h1>Booking confirmations</h1><p>After you have verified the bank transfer in WhatsApp, confirm the booking here. Confirmed dates are locked on the availability calendar.</p><div className="stats"><div className="stat"><span>Awaiting payment</span><strong>{bookings.filter(b=>b.status==="awaiting_payment").length}</strong></div><div className="stat"><span>Payment review</span><strong>{bookings.filter(b=>b.status==="payment_review").length}</strong></div><div className="stat"><span>Confirmed</span><strong>{bookings.filter(b=>b.status==="confirmed").length}</strong></div><div className="stat"><span>All bookings</span><strong>{bookings.length}</strong></div></div><div className="table">{bookings.length===0?<p>No booking requests yet.</p>:<table><thead><tr><th>Reference</th><th>Guest</th><th>Stay dates</th><th>Payment</th><th>Status</th><th>Action</th></tr></thead><tbody>{bookings.map(b=><tr key={b.id}><td>{b.id}</td><td>{b.name}<br/><small>{b.phone}</small></td><td>{b.checkIn}<br/>{b.checkOut}</td><td>{b.payment}</td><td><span className="status">{b.status.replaceAll("_"," ")}</span></td><td>{b.status!=="confirmed"&&<button className="button" onClick={()=>updateBooking(b.id,"confirmed")}>Confirm & lock dates</button>}</td></tr>)}</tbody></table>}</div></>}
