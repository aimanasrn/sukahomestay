import Link from "next/link"; import { BedDouble, Bath, Users } from "lucide-react"; import type { Unit } from "@/types";
export function UnitCard({unit}:{unit:Unit}){return <article className="card"><figure><img src={unit.image} alt={`Exterior of ${unit.name}`} /></figure><div className="card-body"><span className="eyebrow">{unit.type.replace("_"," ")}</span><h3>{unit.name}</h3><p>{unit.shortDescription}</p><div className="meta"><span><BedDouble size={14}/> {unit.bedrooms} bed</span><span><Bath size={14}/> {unit.bathrooms} bath</span><span><Users size={14}/> {unit.maximumGuests}</span></div><div className="price">From RM {unit.basePrice} <small>/ night</small></div><Link className="button secondary" href={`/accommodations/${unit.slug}`}>View details</Link></div></article>}
