"use client";
import { useRouter } from "next/navigation";
import { FormEvent } from "react";
export function BookingSearch(){const router=useRouter();function search(e:FormEvent<HTMLFormElement>){e.preventDefault();const q=new URLSearchParams(new FormData(e.currentTarget) as never);router.push(`/availability?${q.toString()}`)}return <form className="search" onSubmit={search}><div className="field"><label htmlFor="unit">Accommodation</label><select id="unit" name="unit" defaultValue=""><option value="">Choose your stay</option><option>Full Homestay</option><option>Roomstay 1</option><option>Roomstay 2</option><option>Roomstay 3</option><option>Whole House</option></select></div><div className="field"><label htmlFor="checkin">Check-in</label><input id="checkin" name="checkin" type="date" required/></div><div className="field"><label htmlFor="checkout">Check-out</label><input id="checkout" name="checkout" type="date" required/></div><div className="field"><label htmlFor="guests">Guests</label><select id="guests" name="guests" defaultValue="2"><option value="1">1 guest</option><option value="2">2 guests</option><option value="4">4 guests</option><option value="8">8 guests</option></select></div><button className="button" type="submit">Search</button></form>}
